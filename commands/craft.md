---
description: Build a new page from a brief, or improve an existing HTML file, with the editorial-web-craft skill set — audit, browser preview, iterate.
argument-hint: "[brief, or path/to/page.html]"
allowed-tools: ["Read", "Write", "Edit", "Bash", "Glob", "Grep", "Skill", "mcp__*chrome-devtools__*"]
---

Load the `web-craft` skill with the Skill tool and carry out its full workflow on this input:

$ARGUMENTS

If the input is a path to an existing file, this is an **improve** task: audit and preview it first, list what fails, then fix. Otherwise it is a **new** page from the brief. Finish with the report format the skill specifies.
